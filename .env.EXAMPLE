# Environment variables required by this Hermes distribution.
# Copy to `.env` and fill in your own values before running.

# API key for gin model access
# (required)
GIN_API_KEY=

# Base URL for gin model provider
# (required)
GIN_BASE_URL=
